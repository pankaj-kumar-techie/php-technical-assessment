# Laravel User Registration

A simple user registration system built with Laravel.

## Setup
Run `./setup.sh` to set up the Docker environment.

## Features
- User registration with input validation
- Secure password hashing
- CSRF protection
